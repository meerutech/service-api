---
swagger: "2.0"
info:
  version: "dev"
  title: "apigateway-demo"
basePath: "/prod"
schemes:
- "https"
paths:
  /python-apurva:
    get:
      produces:
      - "application/json"
      responses:
        200:
          description: "200 response"
          schema:
            $ref: "#/definitions/Empty"
      x-amazon-apigateway-integration:
        # Dynamically pull down and set URI
        uri: "http://a53e9fde143bb11eaa6f706cbc48cd54-2c281d485ac5c852.elb.us-east-2.amazonaws.com"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        connectionType: "VPC_LINK"
        # Should use stage variables when possible, connectionId is one
        connectionId: "zfy5y2"
        #connectionId: "${stageVariables.vpcLinkId}"
        httpMethod: "GET"
        type: "http_proxy"
definitions:
  Empty:
    type: "object"
    title: "Empty Schema"
